CREATE TABLE product (
                         id                  varchar(80) PRIMARY KEY,
                         name                varchar(255),
                         stock               integer
);
